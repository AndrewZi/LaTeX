\documentclass[a4paper, 12pt]{article}

\input{../preambule.tex}

\begin{document}
    Текст с красной строки
    \par % Переход на новую строку с красной строки
    Тоже с красной строки
    % Или
    \vspace{10pt}

    % Аналог \par - пустая строка (операции идемпотентны)
    Текст с красной строки
    
    Тоже с красной строки
    \vspace{20pt}

    Текст
    \\ А это уже с новой строчки без красной строки % без правил форматирования (насильственный переход)
    % Или

    Текст \newline А это тоже с новой строчки без красной строки % без правил форматирования (насильственный переход)
    % Или
    \newpage % можно с пробелом - можно без
    А этот текст с новой страницы с красной строки % LaTeX не может соблюсти правила форматирования
    \linebreak Текст с новой строчки без красной строки % с учётом правил форматирования

    \pagebreak
    % Без пробела сначала будет до конца заполнена строчка (если будет (!)), а уже только после её полного заполнения будет переход на следующую страницу
    Опять текст с новой страницы с красной строки.
    \pagebreak
    Не заполнена строка % Перехода на новую страницу нет
\end{document}