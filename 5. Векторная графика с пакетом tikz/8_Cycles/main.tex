\documentclass[a4paper, 12pt]{article}

\input{../preamble.tex}

\usepackage{amsmath} % Для математических окружений и команд, связанных с ними

\usepackage{ifthen} % Для условных операторов

\begin{document}
    \begin{equation*}
        r = 
        \sqrt{\foreach \x in {a, ..., g} { % Поочерёдно присваивать \x буквы от a до g
            \ifthenelse{\equal{\x}{a}}{}{+} % Если x != a, отрисует "+", иначе - нет
            \x^2 % Отрисовывать возведение в квадрат символа, лежащего в \x
        }}
    \end{equation*}
\end{document}